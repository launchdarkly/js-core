name: Build and test shared/common

on:
  push:
    branches: [ main ]
    paths-ignore:
    - '**.md' #Do not need to run CI for markdown changes.
  pull_request:
    branches: [ main ]
    paths-ignore:
    - '**.md'

jobs:
  build-test-common:
    uses: launchdarkly/js-core/.github/workflows/ci.yml@rlamb/sc-191283/add-ci-config
    with:
      workspace_name: '@launchdarkly/js-sdk-common'
      workspace_path: packages/shared/common
