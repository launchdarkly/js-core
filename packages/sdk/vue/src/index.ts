/**
 * This is the API reference for the LaunchDarkly Client-side SDK for Vue.
 *
 * TODO(scaffold): restore the createLDProvider / LDVuePlugin references in the @packageDocumentation
 * summary once those symbols arrive in the next PR.
 *
 * @packageDocumentation
 */
export * from './client';
// TODO(scaffold): the following arrive in the next PR:
//   export { LDVuePlugin, type LDVuePluginOptions } from './plugin';
//   export type { LDClient, LDContext, LDContextStrict, LDOptions, LDEvaluationDetail,
//     LDEvaluationDetailTyped, LDEvaluationReason, LDFlagSet, LDFlagValue, LDInspection,
//     LDLogger, Hook, LDIdentifyOptions, LDIdentifyResult, LDStartOptions,
//     LDWaitForInitializationResult } from '@launchdarkly/js-client-sdk';
