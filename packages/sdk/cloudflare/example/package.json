{
  "name": "@launchdarkly/cloudflare-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "module": "./dist/index.mjs",
  "dependencies": {
    "@launchdarkly/cloudflare-server-sdk": "2.7.27"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230321.0",
    "@types/jest": "^29.5.5",
    "jest": "^29.7.0",
    "jest-environment-miniflare": "^2.5.0",
    "miniflare": "^2.5.0",
    "ts-jest": "^28.0.3",
    "tsup": "^8.5.1",
    "typescript": "5.1.6",
    "wrangler": "3.114.17"
  },
  "scripts": {
    "build": "tsup",
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "yarn build && jest",
    "clean": "rm -rf dist && rm -rf node_modules && rm -rf .yarn/cache && yarn build"
  }
}
