const fs = require('fs');
const path = require('path');

const value = process.env.LD_CLIENT_SIDE_ID || '';
const content = `// Auto-generated by scripts/inject-ld-id.js - set LD_CLIENT_SIDE_ID when running yarn start / yarn build.
export const LD_CLIENT_SIDE_ID = ${JSON.stringify(value)};
`;

const outPath = path.join(__dirname, '../src/ld-config.ts');
fs.writeFileSync(outPath, content);
