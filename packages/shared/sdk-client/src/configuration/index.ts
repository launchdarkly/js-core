import Configuration, {
  DEFAULT_BASE_URI,
  DEFAULT_EVENTS_URI,
  DEFAULT_STREAM_URI,
} from './Configuration';

export { DEFAULT_BASE_URI, DEFAULT_EVENTS_URI, DEFAULT_STREAM_URI };
export default Configuration;
